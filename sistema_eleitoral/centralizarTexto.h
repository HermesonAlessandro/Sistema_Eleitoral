#ifndef CENTRALIZAR_TEXTO_H // Verifica se a macro CENTRALIZAR_TEXTO_H já foi definida
#define CENTRALIZAR_TEXTO_H // Diretiva do pré-processador que define a macro CENTRALIZAR_TEXTO_H

#include <string> // Usado para a criação de classes do tipo string
using namespace std; // Biblioteca padrão do c e c++

void centralizarTexto(string texto); // Recebe um argumento do string e não retorna da porque é vazio
#endif // CENTRALIZAR_TEXTO_H // Isso encerra a condição #ifndef
